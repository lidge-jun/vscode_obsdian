# Phase 8.1: HWP/HWPX — Hardened Diff Plan

Created: 2026-05-28
Parent: `08_phase_08_hwp_hwpx_native_support.md`
Status: Audit FAIL → Hardened re-plan

## Audit Findings & Fixes

| # | Issue | Severity | Fix |
|---|-------|----------|-----|
| 1 | `Hwp.tsx` imports `onMessage/postMessage` — doesn't exist in `vscode.ts` | **CRITICAL** | Rewrite to use `handler.on()/emit()` pattern (like Pptx.tsx) |
| 2 | `btoa(String.fromCharCode(...hwpBytes))` stack overflow on large files | **CRITICAL** | Use `[...new Uint8Array(buffer)]` array pattern (like Excel save) |
| 3 | `.hwpx` save overwrites original with HWP bytes → format corruption | **CRITICAL** | `.hwp` → overwrite original, `.hwpx` → save as sibling `.hwp` |
| 4 | `package-lock.json` not in MODIFY list | **MINOR** | Added (auto-modified by `npm install`) |
| 5 | `import { RhwpEditor }` — class not exported at runtime | **MINOR** | Use `import type { RhwpEditor }` |
| 6 | `fs.writeFile` instead of `workspace.fs.writeFile` | **MINOR** | Use `workspace.fs.writeFile` + reuse `save`/`saveDone` event pattern |
| 7 | CSP — no explicit diff in plan | **INFO** | No CSP exists currently; leave as QA risk, document fallback |

## Decisions (Boss)

- **HWPX save**: `.hwpx` 파일 편집 후 Save → 같은 디렉토리에 `filename.hwp` sibling 생성. 원본 `.hwpx` 보존.
- **Offline bundle**: Stage 2, 별도 Phase. Phase 8 범위 밖.
- **CSP**: Stage 1에서 명시적 CSP 추가 안 함. QA에서 iframe 로드 실패 시 Stage 2(로컬 번들)로 즉시 전환.

## File Change Map (Hardened)

### NEW files (3)

```
src/provider/handlers/hwpHandler.ts
src/react/view/hwp/Hwp.tsx
src/react/view/hwp/Hwp.less
```

### MODIFY files (4)

```
package.json                              (dependency + selector)
package-lock.json                         (auto by npm install)
src/provider/officeViewerProvider.ts       (routing + import)
src/react/main.tsx                        (lazy import + route)
```

### DELETE files

없음

---

## Exact Diffs

### 1. `package.json`

```diff
--- a/package.json
+++ b/package.json
@@ dependencies section (line ~43)
+    "@rhwp/editor": "^0.7.13",
```

```diff
--- a/package.json
+++ b/package.json
@@ contributes.customEditors[0].selector (after existing entries, before closing bracket)
+          { "filenamePattern": "*.hwp" },
+          { "filenamePattern": "*.hwpx" }
```

### 2. `package-lock.json`

자동 수정 — `npm install @rhwp/editor` 실행 시 자동 생성. 수동 diff 불필요.

### 3. `src/provider/officeViewerProvider.ts`

```diff
--- a/src/provider/officeViewerProvider.ts
+++ b/src/provider/officeViewerProvider.ts
@@ line 11
 import { handlePptx } from './handlers/pptxHandler';
+import { handleHwp } from './handlers/hwpHandler';
```

```diff
--- a/src/provider/officeViewerProvider.ts
+++ b/src/provider/officeViewerProvider.ts
@@ line 62 (before the .pptx case)
+            case ".hwp":
+            case ".hwpx":
+                route = 'hwp';
+                handleHwp(uri, handler);
+                break;
             case ".pptx":
```

### 4. `src/react/main.tsx`

```diff
--- a/src/react/main.tsx
+++ b/src/react/main.tsx
@@ line 13
 const Pptx = lazy(() => import('./view/pptx/Pptx.tsx'))
+const Hwp = lazy(() => import('./view/hwp/Hwp.tsx'))
```

```diff
--- a/src/react/main.tsx
+++ b/src/react/main.tsx
@@ line 36 (before the pptx case)
+          case 'hwp':
+            return <Hwp />
           case 'pptx':
```

### 5. `src/provider/handlers/hwpHandler.ts` (NEW)

```typescript
import { basename, dirname, extname, join } from 'path';
import { readFileSync } from 'fs';
import { Handler } from '@/common/handler';
import { Uri, workspace } from 'vscode';

export function handleHwp(uri: Uri | { fsPath: string }, handler: Handler): void {
    const fsPath = 'fsPath' in uri ? uri.fsPath : uri.toString();
    const fileUri = Uri.file(fsPath);
    const ext = extname(fsPath).toLowerCase();

    handler.on('init', async () => {
        try {
            const buffer = readFileSync(fsPath);
            const array = [...new Uint8Array(buffer)];
            handler.emit('hwpData', {
                fileName: basename(fsPath),
                buffer: array,
                fileSize: buffer.byteLength,
                isHwpx: ext === '.hwpx',
            });
        } catch (e) {
            handler.emit('hwpData', {
                fileName: basename(fsPath),
                buffer: [],
                fileSize: 0,
                error: e instanceof Error ? e.message : String(e),
            });
        }
    });

    handler.on('saveHwp', async (content: number[]) => {
        try {
            const bytes = new Uint8Array(content);
            let targetUri = fileUri;

            if (ext === '.hwpx') {
                const hwpName = basename(fsPath, ext) + '.hwp';
                targetUri = Uri.file(join(dirname(fsPath), hwpName));
            }

            await workspace.fs.writeFile(targetUri, bytes);
            handler.emit('hwpSaved', {
                success: true,
                savedPath: targetUri.fsPath,
                convertedFromHwpx: ext === '.hwpx',
            });
        } catch (e) {
            handler.emit('hwpSaved', {
                success: false,
                error: e instanceof Error ? e.message : String(e),
            });
        }
    });
}
```

**패턴 근거**:
- `handler.on('init', ...)` + `handler.emit(eventName, data)` — `pptxHandler.ts:21-24` 패턴
- `workspace.fs.writeFile(uri, bytes)` — `commonHandler.ts:27-31` 패턴
- 데이터 전송: `[...new Uint8Array(buffer)]` 숫자 배열 — `excel_writer.ts:74` 패턴
- `.hwpx` sibling 저장 — Boss 결정 (원본 보존)

### 6. `src/react/view/hwp/Hwp.tsx` (NEW)

```tsx
import { Spin, Alert, Button } from 'antd';
import { useEffect, useState, useRef, useCallback } from 'react';
import { createEditor } from '@rhwp/editor';
import type { RhwpEditor } from '@rhwp/editor';
import { handler } from '../../util/vscode.ts';
import './Hwp.less';

interface HwpPayload {
    fileName: string;
    buffer: number[];
    fileSize: number;
    isHwpx: boolean;
    error?: string;
}

interface SaveResult {
    success: boolean;
    savedPath?: string;
    convertedFromHwpx?: boolean;
    error?: string;
}

export default function Hwp() {
    const editorRef = useRef<RhwpEditor | null>(null);
    const containerRef = useRef<HTMLDivElement>(null);
    const [loading, setLoading] = useState(true);
    const [error, setError] = useState<string | null>(null);
    const [saving, setSaving] = useState(false);
    const [saveMsg, setSaveMsg] = useState<string | null>(null);
    const [isHwpx, setIsHwpx] = useState(false);
    const [fileName, setFileName] = useState('');

    useEffect(() => {
        let destroyed = false;

        handler
            .on('hwpData', async (data: HwpPayload) => {
                if (data.error) {
                    setError(data.error);
                    setLoading(false);
                    return;
                }

                setFileName(data.fileName);
                setIsHwpx(data.isHwpx);

                try {
                    if (!containerRef.current) return;
                    const editor = await createEditor(containerRef.current, {
                        width: '100%',
                        height: '100%',
                    });
                    if (destroyed) { editor.destroy(); return; }
                    editorRef.current = editor;

                    const binary = new Uint8Array(data.buffer);
                    await editor.loadFile(binary, data.fileName);
                    setLoading(false);
                } catch (e) {
                    setError(`Failed to load: ${e instanceof Error ? e.message : String(e)}`);
                    setLoading(false);
                }
            })
            .on('hwpSaved', (result: SaveResult) => {
                setSaving(false);
                if (result.success) {
                    const msg = result.convertedFromHwpx
                        ? `Saved as HWP: ${result.savedPath}`
                        : 'Saved successfully';
                    setSaveMsg(msg);
                    setTimeout(() => setSaveMsg(null), 3000);
                } else {
                    setError(`Save failed: ${result.error}`);
                }
            })
            .emit('init');

        return () => {
            destroyed = true;
            editorRef.current?.destroy();
        };
    }, []);

    const handleSave = useCallback(async () => {
        if (!editorRef.current || saving) return;
        setSaving(true);
        try {
            const hwpBytes = await editorRef.current.exportHwp();
            const array = [...new Uint8Array(hwpBytes)];
            handler.emit('saveHwp', array);
        } catch (e) {
            setError(`Export failed: ${e instanceof Error ? e.message : String(e)}`);
            setSaving(false);
        }
    }, [saving]);

    return (
        <div className="hwp-container">
            {error && <Alert type="error" message={error} closable onClose={() => setError(null)} />}
            {saveMsg && <Alert type="success" message={saveMsg} banner />}
            <div className="hwp-toolbar">
                <span className="hwp-filename">{fileName}</span>
                {isHwpx && <span className="hwp-badge">HWPX → saves as HWP</span>}
                <Button
                    type="primary"
                    size="small"
                    onClick={handleSave}
                    loading={saving}
                >
                    Save HWP
                </Button>
            </div>
            {loading && <Spin spinning fullscreen />}
            <div ref={containerRef} className="hwp-editor" />
        </div>
    );
}
```

**패턴 근거**:
- `handler.on(...).on(...).emit('init')` 체인 — `Pptx.tsx:27-30` 패턴
- `import { handler } from '../../util/vscode.ts'` — `Pptx.tsx:3` 패턴
- `Spin spinning fullscreen` — `Pptx.tsx:33` 패턴
- `Alert` for errors — `Pptx.tsx:58` 패턴
- `Button` from antd — 기존 UI 컴포넌트 재사용
- `import type { RhwpEditor }` — type-only import (런타임 export 없음)
- `[...new Uint8Array(hwpBytes)]` — `excel_writer.ts:74` 패턴

### 7. `src/react/view/hwp/Hwp.less` (NEW)

```less
.hwp-container {
    display: flex;
    flex-direction: column;
    width: 100%;
    height: 100vh;
    overflow: hidden;
}

.hwp-toolbar {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 4px 12px;
    background: var(--vscode-editor-background);
    border-bottom: 1px solid var(--vscode-panel-border);
    flex-shrink: 0;
}

.hwp-filename {
    font-size: 12px;
    color: var(--vscode-foreground);
    flex: 1;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.hwp-badge {
    font-size: 11px;
    padding: 1px 6px;
    border-radius: 3px;
    background: var(--vscode-badge-background);
    color: var(--vscode-badge-foreground);
}

.hwp-editor {
    flex: 1;
    overflow: hidden;
}

.hwp-editor iframe {
    border: none;
    width: 100%;
    height: 100%;
}
```

---

## Implementation Steps (Build phase)

```text
Step 1: npm install @rhwp/editor  →  package.json + package-lock.json 자동 수정
Step 2: package.json — contributes.customEditors selector에 *.hwp, *.hwpx 추가
Step 3: hwpHandler.ts 작성 (위 diff 그대로)
Step 4: officeViewerProvider.ts — import + .hwp/.hwpx case 추가 (위 diff 그대로)
Step 5: Hwp.tsx + Hwp.less 작성 (위 diff 그대로)
Step 6: main.tsx — lazy import + route case 추가 (위 diff 그대로)
Step 7: npm run build → 빌드 성공 확인
Step 8: npx tsc --noEmit → 타입 체크 확인
Step 9: Extension Development Host에서 QA
```

## QA Checklist

- [ ] `.hwp` 파일 열기 → rhwp-studio 에디터 iframe 표시
- [ ] `.hwpx` 파일 열기 → 동일하게 표시
- [ ] iframe 내 텍스트 편집 가능
- [ ] `.hwp` Save → 원본 파일에 저장, 재오픈 시 내용 유지
- [ ] `.hwpx` Save → sibling `.hwp` 파일 생성, "HWPX → saves as HWP" 안내
- [ ] 기존 PPTX/DOCX/Excel/PDF/이미지 regression 없음
- [ ] 오프라인 시 적절한 에러 메시지 (iframe 로드 실패)
- [ ] `npm run build` 성공
- [ ] `npx tsc --noEmit` 성공

## Risk: CSP iframe block

VS Code WebView가 외부 URL iframe을 차단할 경우:
1. `studioUrl`을 extension 로컬로 변경하는 Stage 2로 즉시 전환
2. rhwp-studio dist를 `resource/hwp/`에 번들 (~15MB)
3. `createEditor(container, { studioUrl: baseUrl + '/hwp/' })`
4. 이 경우 별도 sub-plan (08.2) 작성

현재 extension에 명시적 CSP가 없으므로 WebView 기본 정책이 적용됩니다.
기본 정책에서 iframe 외부 URL이 허용되는지는 QA에서만 확인 가능.
