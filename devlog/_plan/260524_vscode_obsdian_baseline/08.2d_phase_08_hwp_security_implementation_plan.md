# Phase 8.2d: HWP/HWPX Security Implementation Plan

Created: 2026-05-29
Parent: `08.2_phase_08_hwp_security_lifecycle_recovery.md`
Builds on:
- `08.2a_phase_08_hwp_security_exact_diffs.md`
- `08.2b_phase_08_hwp_security_revalidation_fixes.md`
- `08.2c_phase_08_hwp_frontend_revalidation_fixes.md`
Status: PABCD P phase implementation plan

## Part 1 - Easy Explanation

The current HWP screen can show the document, but it trusts an internet iframe
too much and shows editing/saving before VS Code can safely protect the file.

Phase 8.2d makes it safer in one implementation pass:

1. Put the HWP engine inside the extension package instead of loading it live
   from GitHub Pages.
2. Lock the webview with CSP so only extension-owned resources run.
3. Replace loose messages with named, checked HWP messages.
4. Make HWP/HWPX viewer-only by default.
5. Keep save/export behind an explicit experimental setting, with collision
   checks and atomic writes.

Simple version: first we remove the unsafe door, then we add a lock, then we
hide the save button until the full editor lifecycle is ready.

## Current Repository Shape

Compact tree for the touched area:

```text
build.ts
package.json
package-lock.json
resource/
src/common/
  handler.ts
  reactApp.ts
src/provider/
  officeViewerProvider.ts
  handlers/hwpHandler.ts
src/react/
  tsconfig.json
  util/vscode.ts
  util/vscodeConfig.ts
  view/hwp/Hwp.tsx
  view/hwp/Hwp.less
devlog/_plan/260524_vscode_obsdian_baseline/
  08.2*.md
```

Detected conventions:

- Source-of-truth planning lives in `devlog/_plan/260524_vscode_obsdian_baseline/`.
- Structure docs live in `structure/`.
- React routes use `src/react/main.tsx` and route-specific folders under
  `src/react/view/`.
- Extension-host webview routing uses `OfficeViewerProvider` and `ReactApp.view`.
- Existing event transport is string-event based through `Handler` and
  `src/react/util/vscode.ts`; Phase 8.2d validates HWP messages only so other
  routes are not broken.
- Existing build script uses CommonJS in `build.ts`; this plan follows the
  existing file style there.

Docs read and reused:

- `structure/roadmap.md`
- `structure/direction.md`
- `structure/conventions.md`
- `devlog/AGENTS.md`
- Phase 8, 8.1, 8.2, 8.2a, 8.2b, and 8.2c devlog plan files

No new project-level `structure/`, `devlog/`, `docs/`, or `AGENTS.md` folder is
introduced.

## Implementation Policy

### Default feature level

```text
Default: HWP/HWPX viewer-only.
Experimental: save/export hidden unless vscode-obsdian.hwp.experimentalSave is true.
Not implemented in this phase: full editable CustomEditorProvider lifecycle.
```

Reason:

- The current provider is `CustomReadonlyEditorProvider`.
- VS Code dirty state, Cmd+S, Save All, backup, revert, and hot-exit are not
  wired.
- Shipping visible editing as default would still be dishonest even after bridge
  hardening.

### Runtime source

```text
Default runtime: extension-local rhwp-studio bundle.
Blocked default runtime: https://edwardkim.github.io/rhwp/
```

The B phase must create `vendor/rhwp-studio-dist/VERSION.md` with:

```text
upstream: https://github.com/edwardkim/rhwp
npm wrapper: @rhwp/editor@0.7.13
source commit/tag: <exact commit or tag used>
bundle command/source: <exact command or immutable artifact URL>
notes: external live GitHub Pages is not used at runtime
```

If upstream does not expose a reproducible local studio dist, B must stop and
report this as a blocker instead of silently falling back to the live remote URL.

## File Plan

### NEW `src/common/hwpMessageSchema.ts`

Purpose:

- Namespace HWP events.
- Runtime-validate only HWP payloads.
- Share payload types between extension host and React.

Expected complete content shape:

```typescript
export const HWP_EVENTS = {
    init: 'hwp:init',
    fileData: 'hwp:fileData',
    requestSave: 'hwp:requestSave',
    saveResult: 'hwp:saveResult',
    error: 'hwp:error',
} as const;

export type HwpEventName = typeof HWP_EVENTS[keyof typeof HWP_EVENTS];

export interface HwpFileDataPayload {
    fileName: string;
    buffer: number[];
    fileSize: number;
    isHwpx: boolean;
    error?: string;
}

export interface HwpSavePayload {
    bytes: number[];
    sourceFileName: string;
    isHwpx: boolean;
}

export interface HwpSaveResultPayload {
    success: boolean;
    savedPath?: string;
    convertedFromHwpx?: boolean;
    error?: string;
}

export interface HwpErrorPayload {
    error: string;
}

export function isHwpEvent(value: unknown): value is HwpEventName;
export function validateHwpPayload(type: HwpEventName, payload: unknown): boolean;
```

Validation rules:

- `buffer` and `bytes` must be arrays of integers from 0 to 255.
- `fileSize` must be a non-negative number.
- save payload must not be accepted if bytes are empty.
- `hwp:init` accepts `undefined` or an empty payload.
- `hwp:error` requires `{ error: string }`.
- unknown event names are not HWP events and are ignored by this schema.

### NEW `src/react/view/hwp/rhwpBridge/types.ts`

Purpose: define bridge types without importing `@rhwp/editor`.

Expected exports:

```typescript
export const DEFAULT_RHWP_REQUEST_TIMEOUT_MS = 30000;

export type HwpLoadStatusPayload =
    | { status: 'loading' }
    | { status: 'loaded'; pageCount?: number }
    | { status: 'warning'; message: string }
    | { status: 'failed'; message: string };

export interface SecureRhwpEditorOptions {
    studioUrl: string;
    width: string;
    height: string;
    requestTimeoutMs?: number;
    onLoadStatus?: (status: HwpLoadStatusPayload) => void;
}

export interface SecureRhwpEditor {
    loadFile(data: Uint8Array, fileName: string): Promise<unknown>;
    exportHwp(): Promise<ArrayBuffer | Uint8Array | number[]>;
    destroy(): void;
}

export interface RhwpResponse {
    type: 'rhwp-response';
    id: number;
    error?: string;
    result?: unknown;
}
```

### NEW `src/react/view/hwp/rhwpBridge/validateRhwpMessage.ts`

Purpose: validate iframe response envelopes before resolving requests.

Expected behavior:

```typescript
export function validateRhwpResponse(value: unknown): RhwpResponse | undefined {
    if (!value || typeof value !== 'object') return undefined;
    const record = value as Record<string, unknown>;
    if (record.type !== 'rhwp-response') return undefined;
    if (typeof record.id !== 'number') return undefined;
    if ('error' in record && typeof record.error !== 'string') return undefined;
    return record as RhwpResponse;
}
```

The validator must reject unrelated `window.message` traffic.

### NEW `src/react/view/hwp/rhwpBridge/createSecureRhwpEditor.ts`

Purpose:

- Create the local rhwp iframe.
- Verify `event.source === iframe.contentWindow`.
- Verify `event.origin === new URL(studioUrl).origin`.
- Use `postMessage(..., expectedOrigin)`, not `'*'`.
- Use one removable `message` listener.
- Use 30s default timeout.
- Wait for iframe `load`, then poll `ready` before `loadFile`.
- Clear each request timer on resolve, reject, timeout, and `destroy()`.
- Set the iframe sandbox policy explicitly; start with
  `allow-scripts allow-same-origin allow-forms allow-downloads` and record any
  CSP/sandbox relaxation required by the WASM spike.
- Keep `loadFile` protocol compatible with `@rhwp/editor@0.7.13` by sending
  `Array.from(data)`.

Important pseudo-diff:

```diff
- import { createEditor } from '@rhwp/editor';
+ import { createSecureRhwpEditor } from './rhwpBridge/createSecureRhwpEditor';
```

Request shape:

```typescript
await waitForIframeLoad();
await waitReady();
request('loadFile', { data: Array.from(data), fileName });
request('exportHwp');
```

Destroy must:

```text
removeEventListener('message', onMessage)
reject or clear pending requests
remove iframe from DOM
```

### NEW `vendor/rhwp-studio-dist/VERSION.md`

Purpose: record provenance for the local rhwp studio bundle.

The B phase also adds the built dist files under `vendor/rhwp-studio-dist/`.
This is a vendored artifact directory, not hand-written source.

B-phase bootstrap prerequisite:

```text
mkdir -p vendor
git clone https://github.com/edwardkim/rhwp vendor/rhwp-src
cd vendor/rhwp-src
git checkout v0.7.13
cd rhwp-studio
npm install
npm run build
copy rhwp-studio/dist/* -> vendor/rhwp-studio-dist/
```

Pinned tag from 8.2b: `v0.7.13`
(`b3e16ef212af81ef37d973ddb86d6816d3804642`). If this tag or build output is
not reproducible, stop B and report the blocker.

### MODIFY `build.ts`

Add copy asset before existing copy plugins:

```diff
 const sharedPlugins = [
+    copy({
+        resolveFrom: 'cwd',
+        assets: {
+            from: ['./vendor/rhwp-studio-dist/**/*'],
+            to: ['./resource/rhwp-studio/'],
+            keepStructure: true
+        },
+    }),
     copy({
```

Acceptance:

```text
npm run build creates resource/rhwp-studio/index.html
npm run package includes extension/resource/rhwp-studio/index.html
```

### MODIFY `package.json`

Add activation events:

```diff
 "activationEvents": [
-    "onStartupFinished"
+    "onStartupFinished",
+    "onCustomEditor:cweijan.officeViewer",
+    "onCustomEditor:cweijan.imageViewer",
+    "onCustomEditor:cweijan.markdownViewer",
+    "onCustomEditor:cweijan.markdownViewer.optional",
+    "onCustomEditor:cweijan.htmlViewer"
 ],
```

Add setting under `contributes.configuration.properties`:

```json
"vscode-obsdian.hwp.experimentalSave": {
  "type": "boolean",
  "default": false,
  "description": "Enable experimental HWP export/save. Disabled by default because HWP editing lifecycle is not yet integrated with VS Code save semantics."
}
```

Do not remove `@rhwp/editor` in this phase unless `npm ls` and build prove it is
unused and removal has no lockfile side effects. The safer B path is to stop
using it in source first and remove it in a later dependency cleanup phase.

### MODIFY `src/common/reactApp.ts`

Extend `ViewOption`:

```diff
 interface ViewOption {
     route: string;
+    rhwpStudioUrl?: string;
+    hwpExperimentalSave?: boolean;
 }
```

Inject CSP after path build:

```diff
- webview.html = this.buildPath(html, webview)
+ webview.html = this.injectCsp(this.buildPath(html, webview), webview)
```

Production CSP target:

```text
default-src 'none';
img-src ${webview.cspSource} data: blob:;
font-src ${webview.cspSource} data:;
style-src ${webview.cspSource} 'unsafe-inline';
script-src ${webview.cspSource};
connect-src ${webview.cspSource};
frame-src ${webview.cspSource};
worker-src ${webview.cspSource} blob:;
```

Dev CSP may allow `http://127.0.0.1:5739` and `ws://127.0.0.1:5739`; it must
not allow broad `https:` or `*`.

### MODIFY `src/provider/officeViewerProvider.ts`

Add local rhwp resource root:

```diff
+ const rhwpStudioRoot = vscode.Uri.file(`${this.extensionPath}/resource/rhwp-studio`)
  webview.options = {
      enableScripts: true,
-     localResourceRoots: [vscode.Uri.file(this.extensionPath), folderPath, this.context.globalStorageUri]
+     localResourceRoots: [
+         vscode.Uri.file(this.extensionPath),
+         rhwpStudioRoot,
+         folderPath,
+         this.context.globalStorageUri
+     ]
  }
```

Pass HWP route config:

```diff
- if (route) return ReactApp.view(webview, { route })
+ if (route === 'hwp') {
+     const rhwpStudioUrl = webview.asWebviewUri(
+         vscode.Uri.file(`${this.extensionPath}/resource/rhwp-studio/index.html`)
+     ).toString();
+     const hwpExperimentalSave = vscode.workspace
+         .getConfiguration('vscode-obsdian')
+         .get<boolean>('hwp.experimentalSave', false);
+     return ReactApp.view(webview, { route, rhwpStudioUrl, hwpExperimentalSave });
+ }
+ if (route) return ReactApp.view(webview, { route })
```

### MODIFY `src/provider/handlers/hwpHandler.ts`

Changes:

- Remove `readFileSync`.
- Use `workspace.fs.readFile`.
- Emit `HWP_EVENTS.fileData`.
- Guard save listener behind `vscode-obsdian.hwp.experimentalSave`.
- Validate file size and file magic.
- Save `.hwpx` export as `*.converted.hwp` by default.
- If target exists, prompt before overwrite or ask user for a path.
- Use temp write plus rename/copy cleanup for safer writes.

Constants:

```typescript
const MAX_HWP_BYTES = 50 * 1024 * 1024;
const OLE_MAGIC = [0xd0, 0xcf, 0x11, 0xe0];
const ZIP_MAGIC = [0x50, 0x4b, 0x03, 0x04];
```

Event changes:

```diff
- handler.on('init', async () => {
+ handler.on(HWP_EVENTS.init, async () => {

- handler.emit('hwpData', {
+ handler.emit(HWP_EVENTS.fileData, {

- handler.on('saveHwp', async (content: number[]) => {
+ const experimentalSave = workspace
+     .getConfiguration('vscode-obsdian')
+     .get<boolean>('hwp.experimentalSave', false);
+ if (experimentalSave) {
+     handler.on(HWP_EVENTS.requestSave, async (content: HwpSavePayload) => {
+         // guarded experimental save implementation
+     });
+ }

- handler.emit('hwpSaved', {
+ handler.emit(HWP_EVENTS.saveResult, {
```

Remaining React transport, frontend UI, TypeScript, verification, and employee
audit details are split into
`08.2e_phase_08_hwp_security_frontend_verification_plan.md` to keep each
Jawdev plan file under 500 lines.
